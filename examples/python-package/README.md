# Python package boundary example

```bash
PYTHONPATH=src python -m boundver.core generate \
  --config examples/python-package/boundary.config.json \
  --out examples/python-package/expected.boundary.lock.json \
  --source working-tree \

```
